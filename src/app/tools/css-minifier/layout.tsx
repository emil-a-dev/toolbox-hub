import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CSS Minifier / Beautifier — Minify & Format CSS', description: 'Free online CSS minifier and beautifier. Compress or format CSS code with size comparison.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
