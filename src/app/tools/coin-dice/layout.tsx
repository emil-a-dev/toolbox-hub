import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Coin Flip & Dice Roller — Random Decisions', description: 'Flip a coin or roll dice online. Make random decisions with a click.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
