import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'NATO Phonetic Alphabet Converter', description: 'Convert text to NATO phonetic alphabet. Full alphabet reference included.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
