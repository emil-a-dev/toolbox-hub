import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'ROT13 Encoder/Decoder — Cipher Tool', description: 'Apply ROT13 substitution cipher to encode or decode text.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
