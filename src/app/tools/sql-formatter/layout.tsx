import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SQL Formatter — Format & Beautify SQL', description: 'Free online SQL formatter. Beautify, format, and minify SQL queries.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
