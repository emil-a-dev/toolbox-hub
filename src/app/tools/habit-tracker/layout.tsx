import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Habit Tracker — Daily Habit Tracking', description: 'Track daily habits with a simple visual tracker. Data saved locally in your browser.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
