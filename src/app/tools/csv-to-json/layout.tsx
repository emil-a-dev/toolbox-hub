import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CSV to JSON Converter', description: 'Convert CSV data to JSON array format online. Auto-detects headers and data types.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
