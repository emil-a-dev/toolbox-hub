import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Invoice Generator — Create Invoices', description: 'Create and print professional invoices online. Free invoice generator with tax calculation.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
