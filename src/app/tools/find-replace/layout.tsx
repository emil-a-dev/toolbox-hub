import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Find & Replace — Text Search with Regex', description: 'Free online find and replace tool with regex support. Search and replace text instantly.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
