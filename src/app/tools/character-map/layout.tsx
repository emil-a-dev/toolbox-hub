import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Character Map — Special Symbols & Characters', description: 'Browse and copy special characters, symbols, arrows, currency signs, and more.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
