import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Lorem Picsum — Placeholder Image Generator', description: 'Generate placeholder images with custom dimensions, blur, grayscale. Get image URLs and HTML.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
