import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Number Base Converter — DEC, BIN, OCT, HEX', description: 'Convert numbers between decimal, binary, octal, and hexadecimal bases.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
