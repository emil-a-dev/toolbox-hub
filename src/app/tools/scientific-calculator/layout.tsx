import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Scientific Calculator Online', description: 'Full-featured scientific calculator with trig, log, power functions and more.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
