import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Kanban Board — Task Management', description: 'Simple Kanban board for task management. Drag and drop tasks between columns.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
