import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Meeting Cost Calculator — Real-Time', description: 'Calculate meeting cost in real-time based on attendees and salaries.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
