import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Structured Data Generator — JSON-LD Schema', description: 'Generate JSON-LD structured data for SEO. Support for Organization, Article, Product, FAQ, and more.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
