import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Punycode Converter — IDN Domain Encoder', description: 'Convert international domain names to and from Punycode format.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
