import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'HTML Entity Encoder/Decoder', description: 'Encode or decode HTML entities online. Convert special characters to HTML entities and back.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
