import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Aspect Ratio Calculator', description: 'Calculate aspect ratios and resize dimensions proportionally with common presets.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
