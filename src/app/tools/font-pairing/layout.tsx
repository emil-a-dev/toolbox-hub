import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Font Pairing Suggester — Google Font Pairings', description: 'Find beautiful font pairings with live preview. Google Fonts combinations for headings and body text.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
