import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Word Frequency Counter — Analyze Text', description: 'Count word frequency in text. Find the most common words with visual bar chart.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
