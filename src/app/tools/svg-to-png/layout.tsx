import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'SVG to PNG Converter — Convert SVG to PNG', description: 'Convert SVG code to downloadable PNG images with adjustable scale.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
