import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Meta Tag Generator — SEO & Open Graph Tags', description: 'Generate meta tags for SEO and social media. Includes Open Graph and Twitter Card tags.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
