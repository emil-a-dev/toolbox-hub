import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Byte Converter — KB, MB, GB, TB', description: 'Convert between bytes, kilobytes, megabytes, gigabytes, terabytes, and bits.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
