import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'XML Sitemap Generator', description: 'Generate XML sitemaps for better search engine indexing. Enter URLs and configure settings.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
