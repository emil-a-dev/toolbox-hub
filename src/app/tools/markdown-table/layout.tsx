import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Markdown Table Generator', description: 'Generate Markdown tables visually. Set columns, rows, alignment, and copy the output.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
