import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Emoji Picker — Search & Copy Emojis', description: 'Browse, search, and copy emojis with one click. Organized by category.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
