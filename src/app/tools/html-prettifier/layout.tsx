import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'HTML Prettifier — Format & Beautify HTML', description: 'Free online HTML formatter. Beautify or minify HTML code instantly.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
