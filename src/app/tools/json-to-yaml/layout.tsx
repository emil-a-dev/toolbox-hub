import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'JSON to YAML Converter', description: 'Convert JSON to YAML format online. Clean YAML output with proper formatting.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
