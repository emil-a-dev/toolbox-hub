import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Title Case Capitalizer — Text Case Converter', description: 'Convert text to title case, camelCase, snake_case, PascalCase, and more.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
