import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Fake Data Generator — Test Data', description: 'Generate realistic fake data for testing — names, emails, phones, addresses, and more.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
