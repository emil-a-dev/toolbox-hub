import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Glassmorphism Generator — Frosted Glass CSS', description: 'Create beautiful glassmorphism (frosted glass) CSS effects with live preview.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
