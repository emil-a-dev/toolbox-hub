import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Redirect Checker — Check URL Redirects', description: 'Check URL redirects and see the final destination URL.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
