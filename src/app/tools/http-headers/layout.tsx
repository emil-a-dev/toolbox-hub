import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'HTTP Headers Checker — View Response Headers', description: 'Check HTTP response headers of any URL online.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
