import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Code Diff — Compare Code Online', description: 'Free online code diff tool. Compare two code snippets and see added, removed, and unchanged lines.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
