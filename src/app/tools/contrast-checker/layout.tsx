import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Contrast Checker — WCAG Color Accessibility', description: 'Check color contrast ratio for WCAG 2.0 AA and AAA accessibility compliance.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
