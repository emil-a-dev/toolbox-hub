import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Screen Resolution Detector', description: 'Detect your screen resolution, viewport size, pixel ratio, and display info.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
