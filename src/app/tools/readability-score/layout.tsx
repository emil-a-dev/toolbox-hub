import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Readability Score Analyzer — Flesch-Kincaid', description: 'Analyze text readability with Flesch-Kincaid, ARI, and grade level scores.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
