import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'PDF Page Counter — Count Pages in PDF', description: 'Count pages in a PDF file online. Client-side, no file upload needed.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
