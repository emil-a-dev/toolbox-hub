import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Text to Speech — Free Online TTS', description: 'Convert text to speech in your browser. Multiple voices, adjustable speed and pitch.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
