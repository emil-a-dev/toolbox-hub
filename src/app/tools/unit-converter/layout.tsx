import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Unit Converter — Length, Weight, Temperature & More', description: 'Free online unit converter. Convert length, weight, temperature, speed, area, and volume.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
