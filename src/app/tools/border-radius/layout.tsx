import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CSS Border Radius Generator', description: 'Visually create CSS border-radius values with individual corner controls.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
