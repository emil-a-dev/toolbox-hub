import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Statistics Calculator — Mean, Median, StdDev', description: 'Calculate mean, median, mode, standard deviation, variance, and more statistics.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
