import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Text Reverser — Reverse Text Online', description: 'Reverse text, words, or characters online for free.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
