import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Neumorphism Generator — Soft UI CSS', description: 'Create neumorphic (soft UI) CSS effects with live preview and customization.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
