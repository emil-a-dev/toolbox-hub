import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'IP Address Info — What Is My IP', description: 'Find your public IP address and geolocation info including city, country, ISP, and timezone.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
