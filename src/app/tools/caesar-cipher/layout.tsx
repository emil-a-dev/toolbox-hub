import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Caesar Cipher — Encrypt & Decrypt Text', description: 'Encrypt and decrypt text using the Caesar shift cipher with all 25 possible shifts.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
