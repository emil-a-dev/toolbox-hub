import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Typing Speed Test — WPM Test', description: 'Test your typing speed and accuracy. Measure words per minute (WPM) online.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
