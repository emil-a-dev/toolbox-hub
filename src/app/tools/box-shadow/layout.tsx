import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CSS Box Shadow Generator', description: 'Create CSS box shadows visually with live preview, presets, and customizable options.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
