import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'JWT Decoder — Decode JSON Web Tokens', description: 'Free online JWT decoder. Inspect header, payload, and signature of JWT tokens. Check expiration.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
