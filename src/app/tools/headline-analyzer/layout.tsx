import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Headline Analyzer — Score Your Headlines', description: 'Analyze headlines for SEO and engagement. Get a score and improvement suggestions.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
