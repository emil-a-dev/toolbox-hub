import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Reaction Time Test — Test Your Reflexes', description: 'Test your reaction time online. See how fast your reflexes are in milliseconds.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
