import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Random Number Generator', description: 'Generate random numbers within any range. Support for unique numbers and batch generation.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
