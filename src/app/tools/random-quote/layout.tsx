import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Random Quote Generator — Inspirational Quotes', description: 'Get inspired with random quotes from famous people, thinkers, and developers.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
