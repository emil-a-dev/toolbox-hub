import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Morse Code Translator — Encode & Decode', description: 'Translate text to Morse code and back. Listen to Morse code audio.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
