import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Cron Expression Parser — Explain Cron Jobs', description: 'Free cron expression parser. Convert cron expressions to human-readable descriptions.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
