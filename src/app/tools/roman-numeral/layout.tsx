import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Roman Numeral Converter', description: 'Convert between decimal numbers and Roman numerals. Reference table included.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
