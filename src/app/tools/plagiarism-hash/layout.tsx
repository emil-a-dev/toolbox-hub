import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Plagiarism Hash Checker — Compare Texts', description: 'Compare texts using SHA-256 hash fingerprinting and word similarity analysis.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
