import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Expense Tracker — Track Spending', description: 'Track expenses by category with visual breakdown. Data saved locally in your browser.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
