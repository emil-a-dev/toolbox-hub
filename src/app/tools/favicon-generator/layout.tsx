import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Favicon Generator — Create Favicon from Text', description: 'Generate favicons from text. Download in multiple sizes (16, 32, 48, 64, 128, 256).' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
