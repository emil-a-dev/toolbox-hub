import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'BMI Calculator — Body Mass Index', description: 'Free BMI calculator. Calculate your body mass index with metric or imperial units.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
