import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Tip Calculator — Calculate Tips & Split Bills', description: 'Free tip calculator. Calculate tip amount and split the bill between any number of people.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
