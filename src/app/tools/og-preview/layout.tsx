import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Open Graph Preview — Social Share Preview', description: 'Preview how your page will look when shared on Facebook, Twitter, and LinkedIn.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
