import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CSS Grid Generator — Visual Grid Builder', description: 'Create CSS Grid layouts visually and generate the code.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
