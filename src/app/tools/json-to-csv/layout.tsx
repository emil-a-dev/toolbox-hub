import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'JSON to CSV Converter', description: 'Convert JSON arrays to CSV format. Download or copy CSV output.' };
export default function L({ children }: { children: React.ReactNode }) { return children; }
